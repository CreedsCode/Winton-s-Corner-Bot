version: "3.9"
services:
  nginx:
    build:
      context: ./apps/web-presence
    expose:
      - "80"
    restart: unless-stopped
  pgfant:
    build:
      context: ./apps/pgfant
    expose:
      - "80"
    restart: unless-stopped
  # TODO discord-bot:
